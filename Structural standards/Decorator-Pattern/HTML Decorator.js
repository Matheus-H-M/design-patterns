// Define a function named decorateText that takes one parameter called 'text'
function decorateText(text){
    // Return the input text wrapped in <strong> HTML tags (used to make text bold)
    return `<strong>${text}</strong>`;
}

// Log the string "Importante" to the console
console.log("Importante");
